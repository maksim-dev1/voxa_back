# Pinned so a rebuild doesn't silently pick up a torch/ctranslate2 combo
# that stops agreeing with the nexus driver (580.173.02, CUDA 13 API,
# backward-compatible with CUDA 12.x runtime builds below).
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.4.1+cu121
torchaudio==2.4.1+cu121

faster-whisper==1.0.3
pyannote.audio==3.3.2
# pyannote.audio 3.3.2 still calls the old use_auth_token API, removed in
# huggingface_hub 1.x — pin below that or pipeline loading breaks.
huggingface_hub==0.23.4
# Transitive pyannote.audio dep (its VAD module imports it) that pip never
# resolves on its own from pyannote.audio's declared deps. Was masked
# during earlier testing because the pipeline failed on the HF_TOKEN gate
# before it ever reached this import — surfaced only once the gate/license
# issues were fixed and the pipeline actually loaded.
matplotlib==3.9.2
redis==5.0.8
psycopg[binary]==3.2.3
